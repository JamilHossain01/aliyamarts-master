import 'package:flutter/material.dart';

class SiamartScreen extends StatelessWidget {
  const SiamartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("SiamartScreen"),
    );
  }
}
